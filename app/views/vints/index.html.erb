<h1>Listing vints</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Descraption</th>
    <th>Location</th>
    <th>App</th>
    <th>Video url</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @vints.each do |vint| %>
  <tr>
    <td><%= vint.title %></td>
    <td><%= vint.descraption %></td>
    <td><%= vint.location_id %></td>
    <td><%= vint.app_id %></td>
    <td><%= vint.video_url %></td>
    <td><%= link_to 'Show', vint %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Vint', new_vint_path %>
